{
  "private": true,
  "workspaces": [
    "job-portal-client",
    "job-portal-server"
  ],
  "scripts": {
    "postinstall": "cd job-portal-client && npm install && npm run build && cd ../job-portal-server && npm install",
    "prestart": "cd job-portal-client && npm run build",
    "start": "cd job-portal-server && npm start"
  },
  "dependencies": {
    "react-hook-form": "^7.51.2",
    "react-select": "^5.8.0"
  }
}
